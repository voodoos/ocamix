(executable
 (name main)
 (modes js)
 (js_of_ocaml
  (flags
   (:standard --no-inline)))
 (promote
  (only *.bc.js)
  (into ../web)
  (until-clean))
 (flags :standard -open Brrer)
 (libraries
  js_of_ocaml
  brrer
  lwd
  brr-lwd
  ocamix.brr-lwd-ui
  lib
  data_source
  db
  containers-data))
